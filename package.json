{
  "name": "tjappen",
  "version": "1.0.0",
  "description": "Minimalist recipe digital pantry built with Hugo and Tailwind CSS",
  "scripts": {
    "build": "hugo --gc --minify --noTimes",
    "server": "hugo server -D --noTimes"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^3.4.4"
  }
}